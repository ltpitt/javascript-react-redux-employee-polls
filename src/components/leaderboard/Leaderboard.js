import { connect } from "react-redux";

const Leaderboard = () => {
  return (
    <div className="leaderboard">
      <div>Leaderboard</div>
    </div>
  );
};

export default connect()(Leaderboard);
